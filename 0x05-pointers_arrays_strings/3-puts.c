#include "main.h"

/**
 * _puts - writes the string in a new line
 * @str: string
 */

void _puts(char *c)
{
	int len = 0;

	while (char[len] != '\0')
	{
		_putchar(char[len]);
		len++;
	}
	
	_putchar(int '\n');
}
